import { StyleSheet } from 'react-native';

const margin = 14;
const styles = StyleSheet.create({
  containerStyle: {
    alignItems: 'center',
    borderRadius: 5,
    margin,
    height: 50
  },
  textStyle: {
    fontSize: 16,
    margin
  }
})

export { styles }